#include <stdio.h>
/**
 * main - Entry point of program for execution
 *
 * printf - Prints specified text to standard output
 *
 * Return: (0)
 */

int main(void)
{
	printf("%s", "with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
