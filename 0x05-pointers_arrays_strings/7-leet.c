#include "holberton.h"

/**
 * *leet - encode a string into 1337
 * @ch: character to be encoded
 *
 * Return: encoded characters
 */

char *leet(char *ch)
{
	int i;

	i = 0;
	while (ch[i] == 




	return (ch);
}
